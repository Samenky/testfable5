"use client";

/**
 * Grain animé post-processing (Noise OVERLAY, intensity 0.06–0.08).
 * STUB ÉTAPE 0 — implémenté à l'ÉTAPE 1 avec EffectComposer
 * (@react-three/postprocessing) à l'intérieur du Canvas R3F.
 */
export default function GrainEffect() {
  return null;
}
