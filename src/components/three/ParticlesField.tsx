"use client";

/**
 * Système de particules "leads" de la SECTION 4.
 * STUB ÉTAPE 0 — implémenté à l'ÉTAPE concernée
 * (drift brownien → tri en 3 colonnes → condensation en chiffres).
 */
export default function ParticlesField() {
  return null;
}
